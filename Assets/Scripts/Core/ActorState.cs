namespace DefenseDot.Core
{
    /// <summary>
    /// 액터의 현재 상태를 정의합니다.
    /// </summary>
    public enum ActorState
    {
        Idle,
        Moving,
        Attacking,
        Stunned,
        Dead
    }
}
